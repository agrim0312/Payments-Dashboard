{
  "name": "webhook-handler",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "build": "npx esbuild ./src/**/*.ts --bundle --platform=node --outdir=./dist",
    "start": "node dist/bin.js",
    "dev": "npm run build && npm run start",
    "test": "vitest"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "@repo/db": "*",
    "@types/express": "^5.0.0",
    "esbuild": "^0.24.2",
    "express": "^4.21.2",
    "vitest": "^2.1.8"
  },
  "devDependencies": {
    "@types/supertest": "^6.0.2",
    "supertest": "^7.0.0"
  }
}
